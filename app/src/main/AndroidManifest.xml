<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.uniandes.vinilosapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.VinilosApp">
        <activity
            android:name=".views.ArtistsActivity"
            android:exported="false" />
        <activity
            android:name=".views.OpcionesAlbumActivity"
            android:exported="false" />
        <activity
            android:name=".views.ColeccionistaActivity"
            android:exported="false" />
        <activity
            android:name=".views.UsuarioActivity"
            android:exported="false" />
        <activity
            android:name=".views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>